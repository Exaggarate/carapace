apiVersion: v1
kind: Service
metadata:
  name: carapace
  labels:
    app: carapace
spec:
  type: ClusterIP
  selector:
    app: carapace
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
