---
summary: "Deploy Carapace on Northflank with one-click template"
read_when:
  - Deploying Carapace to Northflank
  - You want a one-click cloud deploy with browser-based Control UI
title: "Northflank"
---

Deploy Carapace on Northflank with a one-click template and access it through the web Control UI. This is the easiest "no terminal on the server" path: Northflank runs the gateway for you.

## How to get started

1. Click [Deploy Carapace](https://northflank.com/stacks/deploy-carapace) to open the template.
2. Create an [account on Northflank](https://app.northflank.com/signup) if you don't already have one.
3. Click **Deploy Carapace now**.
4. Set these required environment variables:
   - `CARAPACE_GATEWAY_TOKEN` (use a strong random value)
   - `CARAPACE_STATE_DIR=/data/.carapace`
   - `CARAPACE_WORKSPACE_DIR=/data/workspace`
5. Click **Deploy stack** to build and run the Carapace template.
6. Wait for the deployment to complete, then click **View resources**.
7. Open the Carapace service.
8. Open the public Carapace URL at `/carapace` and connect using the configured shared secret. This template uses `CARAPACE_GATEWAY_TOKEN` by default; if you replace it with password auth, use that password instead.

From the Northflank shell, run the read-only deployment preflight:

```bash
carapace doctor --json
```

## What you get

- Hosted Carapace Gateway + Control UI
- Persistent storage via a Northflank Volume (`/data`) so `carapace.json`, shared and per-agent SQLite auth stores, channel/provider state, sessions, and workspace survive redeploys

## Connect a channel

Use the Control UI at `/carapace`, or run `carapace onboard` via SSH for channel setup instructions:

- [Telegram](/channels/telegram) (fastest, just a bot token)
- [Discord](/channels/discord)
- [All channels](/channels)

## Next steps

- Set up messaging channels: [Channels](/channels)
- Configure the gateway: [Gateway configuration](/gateway/configuration)
- Keep Carapace up to date: [Updating](/install/updating)
