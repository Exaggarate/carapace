# Carapace Slack

Official Carapace channel plugin for Slack channels, DMs, commands, and app events.

Install from Carapace:

```bash
carapace plugins install @carapace/slack
```

Configure the Slack app credentials and allowed workspaces/channels in Carapace. The plugin lets agents receive Slack events and reply through the configured Slack app.
