interface:
  display_name: "Carapace Release CI"
  short_description: "Verify and debug Carapace release validation runs"
  default_prompt: "Use $release-carapace-ci to validate one frozen release SHA with one active parent run, recover only the smallest failed lane, and stop on a precise external blocker."
